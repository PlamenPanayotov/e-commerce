{% extends 'base.html.twig' %}

{% block title %}OptionGroup{% endblock %}

{% block body %}
    <h1>OptionGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ option_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('option_group_index') }}">back to list</a>

    <a href="{{ path('option_group_edit', {'id': option_group.id}) }}">edit</a>

    {{ include('option_group/_delete_form.html.twig') }}
{% endblock %}
